<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.NutriIA" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- 1. TEMA DE ENTRADA (SISTEMA) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_none</item>
        <item name="postSplashScreenTheme">@style/Theme.App.SplashScreen</item>
    </style>

    <!-- 2. TEMA DE TU SPLASH PERSONALIZADO: Pantalla completa -->
    <style name="Theme.App.SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Fondo blanco simple para evitar duplicidad con el logo del layout -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>